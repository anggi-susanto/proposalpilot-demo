export default function BillingPage() {
  return (
    <main>
      <p className="text-sm font-semibold uppercase tracking-[0.2em] text-slate-500">Billing</p>
      <h1 className="mt-3 text-3xl font-bold tracking-tight">Plan and checkout baseline</h1>
      <p className="mt-4 max-w-2xl text-slate-600">
        Empty SSR billing route. Add real plan selection, sandbox checkout action, subscription persistence, and quota ledger here.
      </p>
    </main>
  );
}
