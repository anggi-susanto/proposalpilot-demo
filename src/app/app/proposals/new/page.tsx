export default function NewProposalPage() {
  return (
    <main>
      <p className="text-sm font-semibold uppercase tracking-[0.2em] text-slate-500">New proposal</p>
      <h1 className="mt-3 text-3xl font-bold tracking-tight">Proposal creation baseline</h1>
      <p className="mt-4 max-w-2xl text-slate-600">
        Empty SSR creation route. Add server actions, form validation, generator boundary, save action, and quota decrement here.
      </p>
    </main>
  );
}
