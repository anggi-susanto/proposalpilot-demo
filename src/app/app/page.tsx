export default function DashboardPage() {
  return (
    <main>
      <p className="text-sm font-semibold uppercase tracking-[0.2em] text-slate-500">Dashboard</p>
      <h1 className="mt-3 text-3xl font-bold tracking-tight">Workspace shell</h1>
      <p className="mt-4 max-w-2xl text-slate-600">
        Empty SSR dashboard route. Add server-loaded account, subscription, quota, proposal metrics, and activity feed here.
      </p>
    </main>
  );
}
